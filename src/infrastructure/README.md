# infrastructure

Implementaciones concretas de los ports del dominio:
- `platforms/whatsapp-web/` — `WhatsAppWebAdapter` (deteccion + ocultamiento via DOM).
- `storage/` — repositorio sobre `browser.storage.local`.
